name: Keep Render Backend Awake

on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes (UTC)

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render API to keep alive
        run: |
          curl -s https://your-render-api.onrender.com/api/ping || echo "Ping failed"
